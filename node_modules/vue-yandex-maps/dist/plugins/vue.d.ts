import { VueYandexMaps } from '..';
import { App } from 'vue';

export declare function createYmaps(settings: VueYandexMaps.PluginSettings): {
    install(app: App): void;
};
export declare function createYmapsVue2(settings: VueYandexMaps.PluginSettings): {
    install(Vue: unknown): void;
};
